server:
  port: 8080
  # 静态资源配置
  servlet:
    context-path: /
  resources:
    static-locations: classpath:/static/,file:/uploads/

# 文件上传配置
file:
  upload:
    dir: D:\\uploads
    avatar-dir: D:\\uploads\\avatars
  access:
    prefix: /uploads